server{
  http{
    port=":8008"
  }
  thrift{
    port=":8048"
  }
  admin{
    disable=true
  }
}
database {
  mysql {
    url = "jdbc:mysql://localhost:3306/loanrecord"
    username = "root"
    password = "di@2020!"
  }
}